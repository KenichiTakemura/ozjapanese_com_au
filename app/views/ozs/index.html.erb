<div class="row">
	<div class="row">
		<%= show_notice%>
		<div class="pull-right">
			<div class="btn-toolbar">
				<div class="btn-group">
					<%= link_to_with_icon("#{t("post.back")}",root_path,"btn","","icon-home","")%>
				</div>
				<div class="btn-group">
					<%= post_write_new(@heading, :signin_with_following)%>
				</div>
			</div>
		</div>
	</div>
	<div class="row">
		<%= show_posts(@board_lists, Common.uniqe_token)%>
	</div>
	<%= build_board_list%>
</div>
