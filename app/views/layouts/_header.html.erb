<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container">
			<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </a>
			<a class="brand" href="<%=root_path%>"><i class="icon-fire icon-white"></i><%= t(:project_name)%><%=t(:brisbane)%><small><%=t(:goldcoast)%></small>&nbsp;<%=t(:header_message)%></a>
			<div class="nav-collapse collapse">
				<ul class="nav">
					<li></li>
				</ul>
				<div class="span5 pull-right">
					<div class="navbar-text pull-right">
						<% if controller.controller_name.eql?("terms")%>
							<%=t("signin_agreement_required")%>
						<% else %>
							<strong style="font-size:20px"><%=OzjapaneseStyle.greeting%></strong>
							<% if !current_flyer %>
								<%= signin(:signin_with) %>
							<% else %>
								<%= show_flyer_header %>
							<% end %>
						<% end %>
					</div>
				</div>
			</div><!--/.nav-collapse -->
		</div>
	</div>
</div>
